const CheckForDuplicateEmail = require("../../../middleware/check-for-duplicate-email")

describe("The CheckForDuplicateEmail middleware", () => {
    it("Allows requests with valid emails to go through", () => {
        // TODO: Write unit tests for the CheckForDuplicateEmail middleware
    })

    it("Allows requests without email to go through", () => {
        // TODO: Write unit tests for the CheckForDuplicateEmail middleware
    })

    it("Returns a failed 400 if the email already exists", () => {
        // TODO: Write unit tests for the CheckForDuplicateEmail middleware
    })
})
